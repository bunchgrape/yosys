diff --git a/CMakeLists.txt b/CMakeLists.txt
index 02887a67d..5ae89523b 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -10,14 +10,14 @@ cmake_minimum_required(VERSION 3.28...4.3)
 # is the master project.
 if(NOT DEFINED SLANG_MASTER_PROJECT)
   set(SLANG_MASTER_PROJECT OFF)
-  if(CMAKE_CURRENT_SOURCE_DIR STREQUAL CMAKE_SOURCE_DIR)
+  if(CMAKE_CURRENT_SOURCE_DIR STREQUAL YOSYS_SOURCE_DIR)
     set(SLANG_MASTER_PROJECT ON)
     message(STATUS "CMake version: ${CMAKE_VERSION}")
   endif()
 endif()
 
 # Protect against in-tree builds.
-if(CMAKE_SOURCE_DIR STREQUAL CMAKE_BINARY_DIR)
+if(YOSYS_SOURCE_DIR STREQUAL YOSYS_BINARY_DIR)
   message(
     FATAL_ERROR "In-source builds are not supported. You may need to delete "
                 "'CMakeCache.txt' and 'CMakeFiles/' before building again.")
@@ -259,10 +259,6 @@ if(SLANG_INCLUDE_TOOLS)
   add_subdirectory(tools)
 endif()
 
-if(SLANG_INCLUDE_EXAMPLES)
-  add_subdirectory(examples)
-endif()
-
 if(SLANG_INCLUDE_DOCS)
   add_subdirectory(docs)
 endif()
diff --git a/cmake/gitversion.cmake b/cmake/gitversion.cmake
index f5a3ebeb8..82fd41c27 100644
--- a/cmake/gitversion.cmake
+++ b/cmake/gitversion.cmake
@@ -49,7 +49,7 @@ function(get_git_head_revision _refspecvar _hashvar)
   _git_find_closest_git_dir("${CMAKE_CURRENT_SOURCE_DIR}" GIT_DIR)
 
   if(NOT "${GIT_DIR}" STREQUAL "")
-    file(RELATIVE_PATH _relative_to_source_dir "${CMAKE_SOURCE_DIR}"
+    file(RELATIVE_PATH _relative_to_source_dir "${YOSYS_SOURCE_DIR}"
          "${GIT_DIR}")
     if("${_relative_to_source_dir}" MATCHES "[.][.]")
       # We've gone above the CMake root dir.
diff --git a/external/CMakeLists.txt b/external/CMakeLists.txt
index 0cda11726..c8f31182a 100644
--- a/external/CMakeLists.txt
+++ b/external/CMakeLists.txt
@@ -5,14 +5,18 @@
 
 # --- fmt lib ---
 if(SLANG_USE_SYSTEM_FMT)
-  find_package(fmt 12.2 REQUIRED)
-
-  get_target_property(FMT_CFG fmt::fmt IMPORTED_CONFIGURATIONS)
-  list(GET FMT_CFG 0 FMT_CFG_FIRST)
-  get_target_property(FMT_LIB fmt::fmt IMPORTED_LOCATION_${FMT_CFG_FIRST})
-  get_target_property(FMT_INC fmt::fmt INTERFACE_INCLUDE_DIRECTORIES)
-  message(STATUS "Found system fmt library: ${FMT_LIB}")
-  message(STATUS "Using system fmt include: ${FMT_INC}")
+  if(TARGET fmt::fmt)
+    message(STATUS "Using existing fmt::fmt target")
+  else()
+    find_package(fmt 12.2 REQUIRED)
+
+    get_target_property(FMT_CFG fmt::fmt IMPORTED_CONFIGURATIONS)
+    list(GET FMT_CFG 0 FMT_CFG_FIRST)
+    get_target_property(FMT_LIB fmt::fmt IMPORTED_LOCATION_${FMT_CFG_FIRST})
+    get_target_property(FMT_INC fmt::fmt INTERFACE_INCLUDE_DIRECTORIES)
+    message(STATUS "Found system fmt library: ${FMT_LIB}")
+    message(STATUS "Using system fmt include: ${FMT_INC}")
+  endif()
 
   set(fmt_target "fmt::fmt")
 else()
